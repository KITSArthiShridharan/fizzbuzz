buildscript {
    repositories {
        mavenLocal()
    }
}

plugins {
    id 'eclipse'
    id 'idea'
}

apply plugin: 'java'

group = 'com.kingfisher'
version = 0.1

task sourceJar(type: Jar) {
    from sourceSets.main.allJava
}